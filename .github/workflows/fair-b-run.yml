# Dispatcher for arm B. Pins the reusable workflow by full SHA.
name: fair-b-run
on:
  workflow_dispatch:
    inputs:
      subject_sha:
        required: true
        type: string
      base_sha:
        required: true
        type: string
      approval:
        required: false
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  b:
    uses: kosza123/claim-gate-autopilot/.github/workflows/reusable-fair-b.yml@bf5357d92c5c2cc1de542459fa72464f347431c0
    with:
      subject_sha: ${{ inputs.subject_sha }}
      base_sha: ${{ inputs.base_sha }}
      approval: ${{ inputs.approval }}
